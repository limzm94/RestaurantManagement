<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurantmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantManagement">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin.Boundary.AdminPage"
            android:label="Admin Function"/>
        <activity android:name=".admin.Boundary.CreateAccount"
            android:label="Create Account"/>
        <activity android:name=".admin.Boundary.EditAccount"
            android:label="Edit Account"/>
        <activity android:name=".owner.OwnerUI"
            android:label="Owner Homepage"/>
        <activity android:name=".customer.CustomerUI"
            android:label="Food Menu" />
        <activity android:name=".customer.CheckoutActivity"
            android:label="Cart Checkout"/>
        <activity android:name=".manager.FoodMenu"
            android:label="Manage Menu"/>
        <activity android:name=".manager.CreateFoodItem"
            android:label="Create new item"/>
        <activity android:name=".manager.ManagerUI"
            android:label="Manager Homepage"/>
        <activity android:name=".manager.EditFoodItem"
            android:label="Edit Food Item"/>
        <activity android:name=".manager.Coupon"
            android:label="Manage Coupon"/>
        <activity android:name=".manager.CreateCoupon"
            android:label="Create Coupon"/>
        <activity android:name=".manager.EditCoupon"
            android:label="Edit Coupon"/>

    </application>

</manifest>